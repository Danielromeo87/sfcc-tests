# ── Scripts de utilidad local (no parte del producto) ───────────────
*.ps1
*.sh
*.bat
*.tmp

# ── API keys y secretos locales ───────────────────────────────────
.deepl-key
.env
.env.*
*.local

# ── Ficheros generados por scripts de traducción ───────────────────
questions-en.json

# ── Override reglas (importante incluir nginx.conf y Dockerfile) ─
!nginx.conf
!Dockerfile
